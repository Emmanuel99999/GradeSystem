@model AcademicGradingSystem.Models.Enrollment
@{
    ViewData["Title"] = "Enroll Student";
}
<h2>Enroll Student</h2>
<form asp-action="Create" method="post">
    @Html.AntiForgeryToken()
    <div class="form-group">
        <label>Student</label>
        <select asp-for="StudentId" class="form-control"
                asp-items="@(new SelectList((IEnumerable<AcademicGradingSystem.Models.User>)ViewData["Students"], "User Id", "Email"))"></select>
    </div>
    <div class="form-group">
        <label>Course</label>
        <select asp-for="CourseId" class="form-control"
                asp-items="@(new SelectList((IEnumerable<AcademicGradingSystem.Models.Course>)ViewData["Courses"], "CourseId", "CourseName"))"></select>
    </div>
    <button type="submit" class="btn btn-primary">Save</button>
    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
</form>